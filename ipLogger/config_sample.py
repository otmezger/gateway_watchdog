# config file. put here your parse.com keys and the objectId of the gateway
# make a copy of this file in your repository and rename it as config.py. this file is ignored on git and wont be published. 

def config():
   C = {
     'APPLICATION_ID': '',
     'REST_API_KEY' : '',
     'GATEWAY_PARSE_ID': ''
   }
   return C
